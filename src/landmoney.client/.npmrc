# Makes the "engines" field in package.json a rule rather than a note. Without
# this npm prints a warning about a wrong Node version and installs anyway,
# which is the same as having no constraint at all.
engine-strict=true
